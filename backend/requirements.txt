# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Database — asyncpg is the async PostgreSQL driver for SQLAlchemy
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Settings and validation
pydantic[email]==2.7.3
pydantic-settings==2.3.4

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# Redis async client
redis[asyncio]==5.0.6

# Needed for FastAPI file upload endpoints (Phase 2)
python-multipart==0.0.9

# HTTP testing client (used in tests, also lets FastAPI test client work async)
httpx==0.27.0